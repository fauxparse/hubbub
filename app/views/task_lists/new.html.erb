<h1>New Task List</h1>

<% form_for @task_list, :url => project_task_lists_path(scope) do |form| %>
	<%= render :partial => "form", :object => form, :locals => { :button => "Create" } %>
<% end %>

<%= link_to 'Back', task_lists_path %>
